#include <stdio.h>

 int main(){
   
  const int size = 5;
  char arr[size];
  printf("Input the chars");
  for(int i = 0; i < size;++i){
   scanf(" %c",&arr[i]);  
  }

  for(int i = size -i; i >= 0 ; --i){
    printf(" %c\n",arr[i]);

  }

  return 0;
 }
