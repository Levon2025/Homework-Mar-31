#include <stdio.h>


 int main(){
  const int size = 5;
  int arr[size];

  printf("Enter the values");
  for(int i = 0; i < size; ++i){
    scanf("%d",&arr[i]);
  }
  for(int i = 0; i < size; ++i){
  if(arr[i] % 2 == 1){
    printf(" %d\n",arr[i]);  
  }
  }
   return 0;

 }
